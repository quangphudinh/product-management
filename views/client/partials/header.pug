include ../mixins/sub-menu.pug
header.header
    .container 
        .row.align-items-center 
            .col-3 
                .inner-logo 
                    a(href = '/')
                        img(src="/images/logo.svg", alt="logo")

            .col-9 
                .inner-menu 
                    ul 
                        li 
                            a(href='/') Trang Chủ 
                        li(class="sub-menu")
                            -const pathProduct = "/products"
                            a(href=pathProduct) Sản Phẩm
                            +sub-menu(layoutProdutsCategory ,pathProduct)
                            

            